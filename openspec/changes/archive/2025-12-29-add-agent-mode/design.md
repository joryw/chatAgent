# Agent 模式设计文档

## Context

### 背景
当前系统支持通过手动开关控制联网搜索,但缺乏智能决策能力。用户需要预判是否需要搜索,且系统无法在搜索结果不足时自主迭代。Agent 模式旨在通过 ReAct 模式让 LLM 自主决策搜索时机和策略。

### 约束条件
- 必须保持现有 Chat 模式的向后兼容性
- 必须使用 LangChain Agent 框架 (已是项目依赖)
- 必须使用 Chainlit UI 框架 (已是项目依赖)
- 需要实时展示 Agent 执行过程
- 需要控制 token 消耗和响应延迟

### 利益相关方
- **最终用户**: 需要更智能的搜索决策和更透明的执行过程
- **开发者**: 需要清晰的架构和易于维护的代码
- **系统**: 需要控制成本和性能

## Goals / Non-Goals

### Goals
1. ✅ 实现 Agent 模式,支持 ReAct 循环
2. ✅ 将搜索功能封装为 LangChain Tool
3. ✅ 在 UI 中实时展示 Agent 执行过程
4. ✅ 提供模式选择器,支持 Chat 和 Agent 两种模式
5. ✅ 保持 Chat 模式的向后兼容性
6. ✅ 实现错误处理和降级策略

### Non-Goals
- ❌ 不支持多 Agent 协作 (未来功能)
- ❌ 不实现自定义工具创建 (未来功能)
- ❌ 不支持工具组合和编排 (未来功能)
- ❌ 不实现 Agent 记忆持久化 (未来功能)
- ❌ 不支持 Agent 之间的消息传递 (未来功能)

## Decisions

### Decision 1: 使用 LangChain ReAct Agent

**选择**: 使用 LangChain 的 `create_react_agent` 或自定义 ReAct Agent

**理由**:
- LangChain 已是项目依赖,无需引入新框架
- ReAct 是经典的 Agent 模式,适合搜索场景
- LangChain 提供完整的 Agent 工具链和 Prompt 模板
- 支持流式输出和中间步骤回调

**替代方案**:
- **OpenAI Function Calling**: 需要更换 LLM (当前支持多提供商)
- **Anthropic Tool Use**: 仅支持 Claude,限制性太大
- **自己实现 Agent**: 开发成本高,维护困难

**决策**: 使用 LangChain ReAct Agent,利用其成熟的实现和社区支持

### Decision 2: 模式切换架构

**选择**: 在应用层 (app.py) 实现模式路由

**架构**:
```python
@cl.on_message
async def on_message(message: cl.Message):
    mode = cl.user_session.get("mode", "chat")
    
    if mode == "chat":
        # 现有的 Chat 模式流程
        await handle_chat_mode(message)
    elif mode == "agent":
        # 新的 Agent 模式流程
        await handle_agent_mode(message)
```

**理由**:
- 清晰的职责分离
- 易于测试和维护
- 不影响现有 Chat 模式代码
- 便于未来扩展更多模式

**替代方案**:
- **Strategy 模式**: 过度设计,当前只有两种模式
- **统一处理**: 会使 Chat 模式代码变复杂

**决策**: 采用简单的 if-else 路由,保持代码简洁

### Decision 3: 工具封装设计

**选择**: 创建 `SearchTool` 类继承 `BaseTool`

**实现**:
```python
from langchain.tools import BaseTool
from typing import Type
from pydantic import BaseModel, Field

class SearchInput(BaseModel):
    """搜索工具的输入模型"""
    query: str = Field(description="搜索查询关键词")

class SearchTool(BaseTool):
    name = "web_search"
    description = "搜索互联网获取实时信息。当需要了解最新事件、实时数据或验证信息时使用。"
    args_schema: Type[BaseModel] = SearchInput
    search_service: SearchService
    
    def _run(self, query: str) -> str:
        """同步执行搜索"""
        # 调用 SearchService
        results = self.search_service.search(query)
        return format_search_results(results)
    
    async def _arun(self, query: str) -> str:
        """异步执行搜索"""
        results = await self.search_service.search_async(query)
        return format_search_results(results)
```

**理由**:
- 符合 LangChain Tool 规范
- 支持同步和异步调用
- 使用 Pydantic 进行输入验证
- 可复用现有 SearchService

**替代方案**:
- **使用 @tool 装饰器**: 不够灵活,难以注入依赖
- **直接传递函数**: 缺少类型检查和文档

**决策**: 采用 BaseTool 继承方式,获得完整的 LangChain 集成

### Decision 4: Agent 执行过程可视化

**选择**: 使用 Chainlit Step 组件展示执行步骤

**UI 结构**:
```
┌─────────────────────────────────────┐
│ 💭 思考: 用户想知道...              │  <- Step 1 (auto-collapse)
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 🔍 搜索: "OpenAI GPT-4 latest news" │  <- Step 2 (collapsed)
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 📝 搜索结果: 找到 5 条结果          │  <- Step 3 (collapsed)
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 💭 思考: 结果显示...                │  <- Step 4 (auto-collapse)
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 📄 最终回答: 根据最新消息...        │  <- Final answer (expanded)
└─────────────────────────────────────┘
```

**实现方式**:
```python
async with cl.Step(name="思考中...", type="tool") as step:
    step.output = reasoning_content
    # 自动折叠
    step.collapsed = True

async with cl.Step(name="搜索", type="tool") as step:
    step.input = search_query
    step.output = search_results
    step.collapsed = True
```

**理由**:
- Chainlit Step 组件专为步骤展示设计
- 支持展开/折叠功能
- 支持流式更新
- 提供清晰的视觉层次

**替代方案**:
- **使用普通消息**: 无法折叠,信息过载
- **使用自定义组件**: 开发成本高,兼容性差

**决策**: 使用 Chainlit Step,最大化利用框架能力

### Decision 5: 错误处理和降级策略

**策略**:

1. **Agent 执行超时** (超过 60 秒):
   - 停止 Agent 执行
   - 返回已有的中间结果
   - 提示用户切换到 Chat 模式

2. **工具调用失败**:
   - 记录错误日志
   - 向 Agent 返回错误信息
   - 让 Agent 决定是否重试或使用已有知识

3. **Agent 无限循环** (超过 5 次迭代):
   - 强制停止 Agent
   - 返回最后一次的思考结果
   - 建议用户重新提问或切换模式

4. **LLM API 错误**:
   - 使用现有的重试机制
   - 如果重试失败,显示友好错误消息
   - 不降级到 Chat 模式 (用户已选择 Agent 模式)

**理由**:
- 保证用户体验,避免长时间等待
- 给予 Agent 一定的容错能力
- 防止失控导致成本激增

### Decision 6: 配置管理

**新增环境变量**:
```bash
# Agent 模式配置
AGENT_MAX_ITERATIONS=5          # 最大迭代次数
AGENT_MAX_EXECUTION_TIME=60     # 最大执行时间(秒)
AGENT_VERBOSE=true              # 是否输出详细日志
DEFAULT_MODE=chat               # 默认模式: chat|agent
```

**配置模型**:
```python
class AgentConfig(BaseModel):
    max_iterations: int = Field(default=5, ge=1, le=10)
    max_execution_time: int = Field(default=60, ge=10, le=300)
    verbose: bool = Field(default=True)
    
    class Config:
        env_prefix = "AGENT_"
```

**理由**:
- 使用 Pydantic 进行验证,保证配置安全
- 提供合理的默认值和边界检查
- 便于不同环境的配置管理

## Risks / Trade-offs

### Risk 1: Token 消耗增加
**风险**: Agent 模式的多轮思考和搜索会显著增加 token 消耗

**影响**: 
- 使用成本可能翻倍或更多
- 某些用户可能因成本考虑放弃使用

**缓解措施**:
1. ✅ 限制最大迭代次数 (默认 5 次)
2. ✅ 在 UI 中显示预计 token 消耗
3. ✅ 提供 Chat 模式作为经济替代方案
4. ✅ 在文档中明确说明成本差异

### Risk 2: 响应延迟增加
**风险**: 多轮搜索和思考会增加响应时间

**影响**:
- 用户等待时间可能达到 30-60 秒
- 可能降低用户满意度

**缓解措施**:
1. ✅ 实时展示执行进度,减少焦虑
2. ✅ 设置最大执行时间限制
3. ✅ 优化搜索服务性能
4. ✅ 考虑使用更快的模型 (如 DeepSeek)

### Risk 3: Agent 行为不可预测
**风险**: Agent 可能做出意外决策或陷入循环

**影响**:
- 用户体验不一致
- 难以调试和优化

**缓解措施**:
1. ✅ 精心设计 Agent Prompt,明确行为规范
2. ✅ 实现严格的停止条件
3. ✅ 记录详细的执行日志
4. ✅ 提供降级到 Chat 模式的选项

### Trade-off 1: 智能 vs 可控
**权衡**: Agent 越智能,行为越不可预测

**决策**: 
- 优先保证可控性和可预测性
- 通过 Prompt Engineering 约束 Agent 行为
- 提供清晰的执行过程可视化
- 允许用户随时切换回 Chat 模式

### Trade-off 2: 功能 vs 复杂度
**权衡**: Agent 功能越强大,系统越复杂

**决策**:
- 第一版只实现基础的 ReAct 循环
- 不实现多 Agent、工具组合等高级功能
- 保持代码简洁,易于维护
- 为未来扩展预留接口

## Migration Plan

### 阶段 1: 准备阶段 (不影响现有功能)
1. 创建 `src/agents/` 目录结构
2. 实现 Agent 相关类,但不在 app.py 中调用
3. 编写单元测试,验证 Agent 逻辑正确
4. 更新依赖 (确保 LangChain 版本支持)

### 阶段 2: 集成阶段 (添加新功能)
1. 在 app.py 中添加模式选择器
2. 实现 Agent 模式处理分支
3. 默认模式仍为 Chat,不影响现有用户
4. 进行内部测试

### 阶段 3: 发布阶段
1. 更新文档,说明两种模式的使用场景
2. 在欢迎消息中介绍 Agent 模式
3. 收集用户反馈
4. 根据反馈优化 Agent 行为

### 回滚计划
如果 Agent 模式出现严重问题:
1. 在 UI 中隐藏 Agent 模式选项
2. 强制所有用户使用 Chat 模式
3. Chat 模式完全独立,不受影响
4. 修复问题后重新启用

### 数据迁移
无需数据迁移。模式选择保存在会话状态中,不涉及持久化数据。

## Open Questions

### Q1: 是否支持用户自定义工具?
**状态**: 待讨论  
**倾向**: 第一版不支持,保持简单  
**原因**: 自定义工具涉及代码执行安全性问题

### Q2: 如何处理 Agent 和 Chat 的对话历史?
**状态**: 待讨论  
**倾向**: 两种模式使用独立的对话历史  
**原因**: Agent 的中间步骤不应污染 Chat 历史

### Q3: 是否需要持久化 Agent 执行日志?
**状态**: 待讨论  
**倾向**: 第一版不持久化,只在当前会话显示  
**原因**: 减少存储成本和隐私问题

### Q4: Agent 模式是否支持所有 LLM 提供商?
**状态**: 需要验证  
**倾向**: 优先支持 OpenAI 和 Anthropic  
**原因**: 这些提供商的函数调用能力更成熟

### Q5: 如何计费或显示 token 使用?
**状态**: 待讨论  
**倾向**: 在每次执行后显示总 token 消耗  
**原因**: 帮助用户了解成本,做出明智选择

